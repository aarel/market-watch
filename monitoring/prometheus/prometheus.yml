global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: market_watch_api
    metrics_path: /metrics
    static_configs:
      - targets: ['127.0.0.1:8000']
